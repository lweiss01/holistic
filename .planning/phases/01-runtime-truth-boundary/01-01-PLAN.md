---
phase: 01-runtime-truth-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/andon-api/src/repository.ts
  - tests/andon.test.ts
autonomous: true
requirements:
  - RTM-01
  - RTM-02
  - RTM-03
  - RTM-04
user_setup: []
must_haves:
  truths:
    - "When runtime rows exist, every fleet card's live status comes from runtime session status + runtime-derived freshness, not from legacy narrative deriveStatus."
    - "needs_input appears only when runtime session status is waiting_for_input."
    - "running/flowing semantics match runtime running/starting with documented heartbeat behavior."
  artifacts:
    - services/andon-api/src/repository.ts
    - tests/andon.test.ts
  key_links:
    - "getFleet runtime branch ↔ listRuntimeSessions / getRuntimeEvents"
---

<objective>
Lock and verify the **runtime-first** `/fleet` classification path (RTM-01–RTM-04) so Mission Control live cards never infer status from legacy narrative events when runtime telemetry is present.

Purpose: Restores operator trust that the board reflects live agent truth.

Output: Green `npm run test:andon` for RTM-focused cases; any minimal code fixes if audits find gaps.
</objective>

<execution_context>
@C:/Users/lweis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/lweis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-runtime-truth-boundary/01-CONTEXT.md
@.planning/phases/01-runtime-truth-boundary/01-RESEARCH.md

@services/andon-api/src/repository.ts
@tests/andon.test.ts
</context>

<tasks>

<task type="auto">
  <name>01-01-01 — Audit getFleet runtime branch for RTM-01/04</name>
  <files>services/andon-api/src/repository.ts</files>
  <action>Trace `getFleet` when `listRuntimeSessions` is non-empty. Confirm fleet `status` / `recommendation` / `supervision` are built only from runtime tables and runtime events (not `deriveStatus`, not legacy `events` tail). Document any accidental legacy path; remove or guard it so live fleet cards cannot inherit narrative inference.</action>
  <verify>grep -n "deriveStatus\\|getEventsTailForRules" services/andon-api/src/repository.ts and confirm neither is used inside the runtime-first `getFleet` branch.</verify>
  <done>No legacy status engine path feeds fleet session items when runtime sessions exist.</done>
</task>

<task type="auto">
  <name>01-01-02 — Verify RTM-02 needs_input mapping</name>
  <files>services/andon-api/src/repository.ts</files>
  <action>Confirm `runtimeStatusToFleetStatus` maps `waiting_for_input` → `needs_input` only, and no other runtime status yields `needs_input`. Align with `hasRuntimeWaitingSignal` usage elsewhere if needed for consistency.</action>
  <verify>npm run test:andon -- --grep "maps runtime waiting_for_input"</verify>
  <done>Test proving needs_input only from runtime waiting passes.</done>
</task>

<task type="auto">
  <name>01-01-03 — Verify RTM-03 running + heartbeat semantics</name>
  <files>services/andon-api/src/repository.ts, tests/andon.test.ts</files>
  <action>Confirm `running`/`starting` map to live running card semantics and `heartbeatFreshness` uses non-heartbeat runtime events (or session updatedAt) per existing tests. Fix only if audit finds regression.</action>
  <verify>npm run test:andon -- --grep "fleet keeps running runtime"</verify>
  <done>Cold freshness + running runtime test passes.</done>
</task>

<task type="auto">
  <name>01-01-04 — Run full Andon test slice</name>
  <files>tests/andon.test.ts</files>
  <action>Run the Andon-focused suite after the above; fix only RTM-related failures introduced by this plan’s edits.</action>
  <verify>npm run test:andon</verify>
  <done>Andon suite green for RTM fleet cases (pre-existing unrelated failures may remain; note them in SUMMARY).</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:andon` passes for RTM fleet tests touched by this wave
- [ ] No `deriveStatus` / legacy event tail in runtime `getFleet` path
</verification>

<success_criteria>
- All tasks completed
- RTM-01, RTM-02, RTM-03, RTM-04 satisfied for **mirrored** runtime fleet rows
- SUMMARY references any remaining unrelated failing tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-truth-boundary/01-01-SUMMARY.md` (or extend existing summaries without deleting `01-02-SUMMARY.md` history).
</output>
