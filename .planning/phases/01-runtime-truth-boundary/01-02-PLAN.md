---
phase: 01-runtime-truth-boundary
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - services/andon-api/src/repository.ts
  - tests/andon.test.ts
autonomous: true
requirements:
  - RTM-01
  - RTM-02
  - RTM-03
  - RTM-04
user_setup: []
must_haves:
  truths:
    - "When at least one runtime session exists, legacy Andon sessions without a runtime_sessions mirror appear as explicit low-urgency parked/disconnected fleet cards with runtime-missing evidence — not omitted and not promoted to active flowing/needs_input via narrative."
    - "Mirrored sessions continue to use strict runtime-only classification from plan 01."
  artifacts:
    - services/andon-api/src/repository.ts
    - tests/andon.test.ts
  key_links:
    - "getFleet merges runtime-backed rows with synthetic/disconnected legacy rows per CONTEXT"
---

<objective>
Implement **mixed fleet visibility**: runtime-backed cards stay strictly runtime-derived; legacy-only rows without a runtime mirror remain **visible** as explicit disconnected/parked entries (per `01-CONTEXT.md`), never receiving live `needs_input` from unresolved narrative alone.

Purpose: Operators see orphan legacy work alongside runtime truth without reintroducing narrative inference for mirrored sessions.

Output: Updated `getFleet` merge logic + regression tests renamed/rewritten to match CONTEXT; `npm run test:andon` green for updated contracts.
</objective>

<execution_context>
@C:/Users/lweis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/lweis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-runtime-truth-boundary/01-CONTEXT.md

@services/andon-api/src/repository.ts
@tests/andon.test.ts
</context>

<tasks>

<task type="auto">
  <name>01-02-01 — Implement legacy-without-runtime-mirror fleet rows</name>
  <files>services/andon-api/src/repository.ts</files>
  <action>Inside the non-empty `listRuntimeSessions` branch, collect legacy `sessions` (or equivalent source used elsewhere) whose ids are **not** present in `runtime_sessions`. For each, append a `FleetSessionItem` with `parked` (or agreed parked/disconnected status), low urgency, explicit explanation/evidence that runtime mirror is missing, and attention semantics consistent with CONTEXT (informational, not competing with urgent runtime rows). Do **not** use `deriveStatus` / narrative events to synthesize `needs_input` or active flowing for these rows.</action>
  <verify>npm run test:andon -- --grep "fleet"</verify>
  <done>Mixed database fixture yields runtime row + disconnected legacy row in `/fleet` payload with correct semantics.</done>
</task>

<task type="auto">
  <name>01-02-02 — Update regression tests for mixed-fleet contract</name>
  <files>tests/andon.test.ts</files>
  <action>Replace or rewrite tests that assert legacy-only rows are **excluded** whenever runtime exists (e.g. names containing “excludes legacy-only sessions whenever runtime sessions exist”) so they assert **visible disconnected/parked** behavior and totals include both classes. Keep tests that assert legacy narrative cannot create `needs_input` without runtime `waiting_for_input`.</action>
  <verify>npm run test:andon</verify>
  <done>No test expects `undefined` for visible legacy-mirror-missing sessions when runtime sessions exist unless CONTEXT explicitly changes.</done>
</task>

<task type="auto">
  <name>01-02-03 — Totals and risk summaries stay consistent</name>
  <files>services/andon-api/src/repository.ts</files>
  <action>Recompute `totals`, `riskReasons`, and sorting so appended disconnected legacy rows do not inflate `activeAgents` / `needsHuman` incorrectly; disconnected rows should rank as low-priority per CONTEXT.</action>
  <verify>npm run test:andon</verify>
  <done>Totals and attention ordering match updated tests.</done>
</task>

<task type="auto">
  <name>01-02-04 — Document delta vs prior SUMMARY if behavior changes</name>
  <files>.planning/phases/01-runtime-truth-boundary/01-02-SUMMARY.md</files>
  <action>If this execution supersedes or corrects an earlier summary, append a short “Plan execution note” dated section to `01-02-SUMMARY.md` or write a fresh dated SUMMARY per template — do not erase historical learnings.</action>
  <verify>test -f .planning/phases/01-runtime-truth-boundary/01-02-SUMMARY.md</verify>
  <done>SUMMARY reflects final mixed-fleet behavior and test names.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:andon` fully green **or** only the two known non-RTM failures from `deferred-items.md` remain, explicitly listed in SUMMARY
- [ ] Manual spot: `/fleet` JSON shows runtime-missing copy on appended legacy cards (optional dev check)
</verification>

<success_criteria>
- CONTEXT mixed-fleet policy implemented and tested
- RTM-04 preserved for mirrored sessions (no narrative live-state injection)
</success_criteria>

<output>
Update or append `.planning/phases/01-runtime-truth-boundary/01-02-SUMMARY.md` after execution.
</output>
