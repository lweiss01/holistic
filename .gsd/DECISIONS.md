# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001/S02 planning | architecture | How S02 should detect natural breakpoints and proactive checkpoint triggers | Use deterministic daemon thresholds (2-hour elapsed or 5 meaningful files) plus explicit checkpoint completion metadata through CLI/MCP instead of transcript parsing or a new watcher. | The existing architecture already has repo-snapshot-based passive capture and checkpoint entrypoints. Deterministic thresholds satisfy R004/R005 without adding another watcher, and explicit completion metadata gives agents a reliable way to express natural breakpoints for R006/R007 while avoiding brittle free-form text inference and draft spam. | Yes | agent |
| D002 | M001 S02 T02 | architecture | How checkpoint entrypoints identify completion-triggered handoff drafting | Require explicit bounded completion metadata (kind + source) on checkpoints, ignore malformed metadata, and share one normalizer/deduped auto-draft helper across daemon, CLI, and MCP flows. | This keeps CLI and MCP behavior aligned, prevents arbitrary free-form text from creating drafts, and centralizes dedupe/reset behavior so proactive triggers stay observable and easier to debug. | Yes | collaborative |
| D003 | M001/S02/T03 | pattern | How agent-facing checkpoint guidance should describe proactive capture | Document explicit supported natural-breakpoint examples and `/checkpoint` / `/handoff` safety valves, while pointing runtime integrations to structured completion metadata instead of implying free-form transcript inference. | S02’s runtime supports bounded completion metadata and explicit manual safety valves, but it does not parse arbitrary conversation semantics. Aligning generated docs, CLI help, and MCP descriptions to those exact surfaces makes proactive capture discoverable without promising unsupported automation. | Yes | agent |
| D004 | M001/S02 | requirement | R007 | validated | S02 verification passed both idle-boundary and explicit completion-signal handoff tests, including duplicate-draft suppression and `handoff --draft` consumption, proving aggressive 30-minute idle and completion-signal auto-drafting behavior. | Yes | agent |
| D005 | M001/S02 | requirement | R004 | validated | S02 verification passed `npm run build && npm test -- --grep "daemon tick\|auto-draft handoff\|holistic_checkpoint"`, including `daemon tick checkpoints on elapsed time with zero changed files`, which proves the daemon checkpoints automatically after the 2-hour boundary even without file changes. | Yes | agent |
| D006 | M001/S02 | requirement | R006 | validated | CLI/MCP completion metadata flows and docs were verified by passing tests for explicit completion-signal drafting plus CLI/MCP help alignment, proving agents can mark supported natural breakpoints with structured narration metadata instead of free-form inference. | Yes | agent |
| D007 | M001/S02 | requirement | R007 | validated | S02 verification passed both idle-boundary and explicit completion-signal handoff tests, including duplicate-draft suppression and `handoff --draft` consumption, proving aggressive 30-minute idle and completion-signal auto-drafting behavior. | Yes | agent |
| D008 | M001/S03 planning | architecture | How archived sessions should reactivate without causing broad archive churn | Use a shared state-layer reactivation helper and limit automatic reactivation to explicit session-id usage: `diff` endpoints always reactivate, handoff references reactivate only exact session-id matches, and a new exact-id `holistic search --id` surface satisfies searched-session behavior. | R009 requires archived sessions to become active again when reused, but current reference fields are free-form strings and broad substring matching would move unrelated sessions back into active storage. Exact-id semantics keep filesystem mutations predictable while still covering diff, handoff, and search workflows through one reusable helper. | Yes | agent |
