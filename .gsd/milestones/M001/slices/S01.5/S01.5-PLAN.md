# S01.5: ASCII Splash Screen & Branding

**Goal:** Add branded ASCII splash screen to CLI and README for visual identity and clear value proposition

**Demo:** Run `holistic init` or `holistic bootstrap` - see HOLISTIC ASCII art with tagline. Open README - see splash screen at top with value prop.

## Must-Haves

- ASCII art renders correctly in terminals (Windows, Mac, Linux)
- Splash shows on `holistic init` and `holistic bootstrap` commands
- README includes splash screen with tagline at top
- Value proposition is clear: "Your repo remembers, so your next agent doesn't have to guess"

## Proof Level

- This slice proves: **contract** — visual identity established, no runtime dependencies
- Real runtime required: **no** — static text rendering
- Human/UAT required: **yes** — verify visual appearance in terminals and GitHub

## Verification

- Run `holistic init` in clean directory - splash screen appears
- Run `holistic bootstrap` - splash screen appears
- View README on GitHub - splash renders correctly
- Test in Windows terminal - no character encoding issues
- Test in Mac/Linux terminal - renders correctly

## Tasks

- [x] **T01: Create splash screen module** `est:30m`
  - Why: Centralize ASCII art and formatting logic
  - Files: `src/core/splash.ts` (new)
  - Do: Create `renderSplash()` function with your ASCII art. Include animated dots for "initializing" effect. Add color support with chalk (optional). Export function for use in CLI commands.
  - Verify: Import and call function, verify output looks correct
  - Done when: Function exists and renders splash correctly

- [x] **T02: Add splash to init command** `est:15m`
  - Why: Show splash when initializing new Holistic repos
  - Files: `src/cli.ts` (handleInit function)
  - Do: Call `renderSplash()` at start of `handleInit()` before any file operations. Show: ASCII art, "initializing shared memory layer..." message, checkmarks for state/docs/hooks installed.
  - Verify: Run `holistic init` in test directory, verify splash appears
  - Done when: Init command shows splash before operations

- [x] **T03: Add splash to bootstrap command** `est:15m`
  - Why: Show splash when bootstrapping new machines
  - Files: `src/cli.ts` (handleBootstrap function)
  - Do: Call `renderSplash()` at start of `handleBootstrap()`. Show same splash with "bootstrapping..." message and checkmarks for hooks/daemon/MCP installed.
  - Verify: Run `holistic bootstrap` in test environment, verify splash appears
  - Done when: Bootstrap command shows splash before operations

- [ ] **T04: Add splash to README** `est:20m`
  - Why: Visual identity and value prop visible in repo
  - Files: `README.md`
  - Do: Add ASCII splash screen to README header (after title, before description). Use markdown code block with proper formatting. Include tagline: "Your repo remembers, so your next agent doesn't have to guess. Shared memory for AI agents, built into your repo." Keep it readable on GitHub (may need to adjust spacing).
  - Verify: View README on GitHub, verify splash renders correctly
  - Done when: README has branded splash with clear value prop

- [x] **T05: Manual UAT - Terminal rendering** `est:10m`
  - Why: Verify splash renders correctly across different terminals
  - Files: none (testing only)
  - Do: Test in Windows terminal (PowerShell, CMD, Git Bash), Mac terminal, Linux terminal (if available). Check for: character encoding issues, line wrapping, color rendering (if colors added).
  - Verify: Splash renders correctly in all tested terminals
  - Done when: No visual issues in any tested environment

## Files Likely Touched

- `src/core/splash.ts` (new)
- `src/cli.ts` (handleInit, handleBootstrap)
- `README.md` (header section)
